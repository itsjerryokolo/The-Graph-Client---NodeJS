{
  "name": "gc-project-one",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Querying a Subgraph using Graph Client",
  "scripts": {
    "start": "node main.js",
    "build": "graphclient build --fileType json"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "@graphprotocol/client-cli": "^2.0.0",
    "graphql": "16.3.0"
  }
}
